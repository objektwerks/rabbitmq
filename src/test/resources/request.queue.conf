amqp {
  url = "amqp://guest:guest@127.0.0.1:5672"
  channel {
    exchange = "request.exchange"
    exchangeType = "direct"
    queue = "request.queue"
    routingKey = "request.queue"
    autoAck = false
  }
}