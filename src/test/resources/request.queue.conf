include "queue.conf"

amqp {
  channel {
    exchange = "request.exchange"
    exchangeType = "direct"
    queue = "request.queue"
    durable = true
    routingKey = "request.queue"
    autoAck = false
    publishConfirmationTimeout = 10000
  }
}