amqp {
  connection {
    url = "amqp://guest:guest@127.0.0.1:5672"
  }
  channel {
    exchange = "test.response.exchange"
    exchangeType = "direct"
    queue = "test.response.queue"
    durable = true
    routingKey = "test.response.queue"
    autoAck = false
    publishConfirmationTimeout = 3000
  }
}